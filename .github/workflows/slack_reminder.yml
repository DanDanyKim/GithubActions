name: App Distributor Reminder

on: push

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: slack reminder
        run: |
          chmod +x ./gradle/scripts/actions/slack.sh
            ./gradle/scripts/actions/slack.sh ${{ secrets.SLACK_WEBHOOK_URL }}
